% Override default figure placement To be within the flow of the text rather
% than on it's own page.
\usepackage{float}
\makeatletter
\def\fps@figure{H} 
\def\fps@tabular{H} 
\def\fps@table{H} 
\makeatother